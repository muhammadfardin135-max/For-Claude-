# Cross-source principles

Claims that more than one ingested source independently supports. These carry
the most weight in generation — independent agreement between curated sources
is the strongest signal this corpus produces.

Format: the principle, the sources that support it, and any source that dissents
(link to `conflicts.md`).

_Nothing yet — needs at least two ingested sources._
